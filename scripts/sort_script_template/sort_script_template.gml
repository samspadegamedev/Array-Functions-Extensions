/// @function sort_ascending(value_1, value_2)

/// @param {?} value_1
/// @param {?} value_2

/// @description The sort script used to sort a list in ? order.

//add error checking if desired - change checks as desired
if (is_nan(argument0)) || (is_nan(argument1)) {
	return false;
}

//return true if value 1 is ? than value 2
return false //the comparison;